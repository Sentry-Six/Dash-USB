#!/bin/bash
# Compatibility stub. Setup runs through the web wizard and auto-resumes after
# interrupted boots.
echo "setup-dashusb is retired: use the Dash USB web wizard at http://dashusb.local" >&2
exit 1
