# Start BLE when late-attaching Broadcom hci0 appears after systemd's initial
# bluetooth.service condition check.
ACTION=="add", SUBSYSTEM=="bluetooth", KERNEL=="hci0", TAG+="systemd", ENV{SYSTEMD_WANTS}+="dashusb-ble.service"
